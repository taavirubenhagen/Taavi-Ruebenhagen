import { selectAllNotes } from "$lib/db/notes";
import { contextButtonFontClass, headingFontClass, paragraphFontClass } from "$state/context";


export async function load({ params }: any) {
  //contextButtonFontClass.set("font-mono");
  //headingFontClass.set("font-mono");
  //paragraphFontClass.set("font-mono");
}